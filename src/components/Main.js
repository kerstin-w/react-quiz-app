/**
 * The Main function renders its children within a main HTML element with the
 * class name "main".
 */
function Main({ children }) {
  return <main className="main">{children}</main>;
}

export default Main;
